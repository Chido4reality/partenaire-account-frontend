<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- MP-CAPACITOR-CAMERA: CameraScanner uses navigator.mediaDevices.getUserMedia
         via zxing for barcode + QR scan (Inventory receive-goods, POS, Refunds).
         Android WebView's runtime permission grant requires the permission to be
         declared here; otherwise getUserMedia rejects without a prompt and the
         scanner silently fails. android:required="false" on the hardware feature
         keeps the APK installable on cameraless devices (tablets, emulators);
         the scanner branches in JS will surface a "no camera" toast instead. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- MP-BT-THERMAL: direct Bluetooth (Classic SPP) ESC/POS receipt printing.
         Legacy BLUETOOTH/BLUETOOTH_ADMIN are install-time and only needed up to
         Android 11; Android 12+ uses the runtime BLUETOOTH_CONNECT (and SCAN).
         neverForLocation declares we don't derive physical location from BT, so
         no location permission is pulled in. Bluetooth is optional hardware so
         the APK still installs on devices without it (the BT print button just
         falls back to the system print dialog). -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" tools:targetApi="s" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
</manifest>
