<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- MP-PLAY-TARGET-SDK-35 / Path A: opt out of Android 15's forced
         edge-to-edge so the WebView keeps opaque status/nav bars and content
         is not drawn under them. Applied to every window theme the activity
         can use. This attribute exists in API 35 (compileSdk 35) and is
         ignored on older devices. Temporary per Google — replace with proper
         env(safe-area-inset-*) handling (Path B) post-launch. -->

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>